Virgil is staged here as the supplied WOFF2 asset. It is not wired into Compose
typography because Android font resources should use a compatible font container
such as TTF/OTF. Convert this asset before promoting it into `res/font`.
